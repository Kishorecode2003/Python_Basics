class Solution:
	def reverse_digit(self, n):
		# Code here
		n=str(n)
		reverse = ''.join(reversed(n))
		reverse=reverse.lstrip('0')
		reverse=int(reverse)
		return reverse
"""
1.used leftstrip to remove zero
2.'' This is a separator. .join is used to concate the string without any space
"""
